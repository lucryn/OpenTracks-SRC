<phone:PhoneApplicationPage
    x:Class="OpenTracksBETA.EqualizerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    SupportedOrientations="Portrait"
    Orientation="Portrait">

    <Grid Background="#FF1D1D1D" Margin="20">
        <StackPanel Margin="0,70,0,0">

            <!-- 🎚️ EQ Sliders -->
            <TextBlock Text="{Binding Path=LocalizedResources.String11, Source={StaticResource LocalizedStrings}}" Foreground="White" FontSize="24" Margin="0,10" />
            <Slider x:Name="BassSlider" Minimum="0" Maximum="100" Value="50" Margin="0,0,0,10" />

            <TextBlock Text="{Binding Path=LocalizedResources.String12, Source={StaticResource LocalizedStrings}}" Foreground="White" FontSize="24" Margin="0,10" />
            <Slider x:Name="MidSlider" Minimum="0" Maximum="100" Value="50" Margin="0,0,0,10" />

            <TextBlock Text="{Binding Path=LocalizedResources.String13, Source={StaticResource LocalizedStrings}}" Foreground="White" FontSize="24" Margin="0,10" />
            <Slider x:Name="TrebleSlider" Minimum="0" Maximum="100" Value="50" Margin="0,0,0,20" />

            <!-- 🎵 Presets -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="{Binding Path=LocalizedResources.String14, Source={StaticResource LocalizedStrings}}" Click="Preset_Pop_Click" Margin="5" />
                <Button Content="{Binding Path=LocalizedResources.String15, Source={StaticResource LocalizedStrings}}" Click="Preset_Bass_Click" Margin="5" />
                <Button Content="{Binding Path=LocalizedResources.String16, Source={StaticResource LocalizedStrings}}" Click="Preset_Treble_Click" Margin="5" />
            </StackPanel>

            <!-- ✅ Apply Button -->
            <Button Content="{Binding Path=LocalizedResources.String17, Source={StaticResource LocalizedStrings}}" Click="ApplyEQ_Click" FontSize="22" Margin="0,20" />
        </StackPanel>
        <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding Path=LocalizedResources.String10, Source={StaticResource LocalizedStrings}}" VerticalAlignment="Top" Height="43" Width="420" Foreground="White" FontSize="30" FontWeight="ExtraLight"/>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" Mode="Minimized">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="back to home" Click="Home_Click"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>
