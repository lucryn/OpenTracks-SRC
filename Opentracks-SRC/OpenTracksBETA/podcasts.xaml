<phone:PhoneApplicationPage
    x:Class="OpenTracksBETA.Podcasts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.Resources>
        <!-- Dark Floating Panel Style -->
        <Style x:Key="FloatingPanelStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background" Value="#CC1E1E1E"/>
            <!-- Dark semi-transparent -->
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <!-- Dark Rounded Button Style -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Background" Value="#FF2C2C2C"/>
            <!-- Dark solid -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}" />
                            <ContentPresenter 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </phone:PhoneApplicationPage.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill" ImageSource="/Assets/bg3.jpg" Opacity="0.4"/>
        </Grid.Background>

        <phone:Panorama Title="openpodcasts" Background="{x:Null}" Foreground="White">
            <phone:PanoramaItem Header="Exclusive Hub">
                <StackPanel Margin="20,-20,20,128" Height="356">
                    <ListBox x:Name="podcastList" ItemsSource="{Binding PodcastList}" SelectionChanged="podcastList_SelectionChanged" Height="408">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource FloatingPanelStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding CoverArt}" Width="80" Height="80" Margin="0,0,10,0"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Foreground="White"/>
                                            <TextBlock Text="{Binding Artist}" FontSize="18" Foreground="#FFCCCCCC"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </phone:PanoramaItem>

            <phone:PanoramaItem Header="About">
                <Border Background="#66121212" CornerRadius="20" Margin="16,-22,16,138" Padding="20">
                    <TextBlock FontWeight="ExtraLight"
                           TextWrapping="Wrap"
                           FontSize="24"
                           Foreground="White">
                    <Run Text="OpenPodcasts is back! Now an exclusive hub for our beloved OpenTracks members. Enjoy free podcasts here!"/>
                    <LineBreak/><LineBreak/>
                    <Run Text="Developed by: ChockingNetDude"/>
                    </TextBlock>
                </Border>
            </phone:PanoramaItem>
        </phone:Panorama>

        <!-- Playback Controls Bar -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30">
            <Button Content="⏮" Style="{StaticResource RoundedButtonStyle}" Click="Rewind_Click"/>
            <Button Content="▶️" Style="{StaticResource RoundedButtonStyle}" Click="Play_Click"/>
            <Button Content="⏸" Style="{StaticResource RoundedButtonStyle}" Click="Pause_Click"/>
            <Button Content="⏭" Style="{StaticResource RoundedButtonStyle}" Click="Forward_Click"/>
        </StackPanel>

        <!-- Media Player -->
        <MediaElement x:Name="mediaPlayer" AutoPlay="False" />
    </Grid>

</phone:PhoneApplicationPage>
