<phone:PhoneApplicationPage
    x:Class="OpenTracksBETA.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Orientation="Portrait">

    <toolkit:TransitionService.NavigationInTransition>
        <toolkit:NavigationInTransition>
            <toolkit:NavigationInTransition.Backward>
                <toolkit:TurnstileFeatherTransition Mode="BackwardIn"/>
            </toolkit:NavigationInTransition.Backward>
            <toolkit:NavigationInTransition.Forward>
                <toolkit:TurnstileFeatherTransition Mode="ForwardIn"/>
            </toolkit:NavigationInTransition.Forward>
        </toolkit:NavigationInTransition>
    </toolkit:TransitionService.NavigationInTransition>

    <toolkit:TransitionService.NavigationOutTransition>
        <toolkit:NavigationOutTransition>
            <toolkit:NavigationOutTransition.Backward>
                <toolkit:TurnstileFeatherTransition Mode="BackwardOut"/>
            </toolkit:NavigationOutTransition.Backward>
            <toolkit:NavigationOutTransition.Forward>
                <toolkit:TurnstileFeatherTransition Mode="ForwardOut"/>
            </toolkit:NavigationOutTransition.Forward>
        </toolkit:NavigationOutTransition>
    </toolkit:TransitionService.NavigationOutTransition>

    <!-- Animation Resources -->
    <phone:PhoneApplicationPage.Resources>
        <local:LocalizedStrings xmlns:local="clr-namespace:OpenTracksBETA" x:Key="LocalizedStrings"/>
        <QuarticEase x:Key="EaseIn" EasingMode="EaseIn"/>

        <Storyboard x:Name="SlideOutStoryboard">
            <DoubleAnimation
                Storyboard.TargetName="SlideTransform"
                Storyboard.TargetProperty="X"
                From="0" To="100"
                Duration="0:0:0.4"
                EasingFunction="{StaticResource EaseIn}"/>
            <DoubleAnimation
                Storyboard.TargetName="LayoutRoot2"
                Storyboard.TargetProperty="Opacity"
                From="1" To="0"
                Duration="0:0:0.4"
                EasingFunction="{StaticResource EaseIn}"/>
        </Storyboard>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot2" Background="Transparent" Opacity="1"
          RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" X="0"/>
        </Grid.RenderTransform>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Panorama control-->
        <phone:Panorama Title="{Binding Path=LocalizedResources.Settings, Source={StaticResource LocalizedStrings}}">
            <phone:Panorama.Background>
                <ImageBrush ImageSource="/Assets/bg1.png"/>
            </phone:Panorama.Background>

            <!--welcome-->
            <phone:PanoramaItem Header="{Binding Path=LocalizedResources.Generals, Source={StaticResource LocalizedStrings}}">
                <StackPanel Margin="16,0,0,0">
                    <TextBlock TextWrapping="Wrap"
                   FontSize="24"
                   Width="400"
                   Height="35"
                   Margin="2,0,2,20">
            <Run Text="{Binding Path=LocalizedResources.String1, Source={StaticResource LocalizedStrings}}"/>
                    </TextBlock>
                    <Button Content="{Binding Path=LocalizedResources.String2, Source={StaticResource LocalizedStrings}}"
                Width="180"
                Height="76"
                FontSize="22"
                HorizontalAlignment="Left"
                Click="ClearCacheButton_Click"/>
                    <TextBlock TextWrapping="Wrap"
                   FontSize="24"
                   Width="400"
                   Height="36"
                   Margin="2,0,2,20">
                    	<Run Text="{Binding Path=LocalizedResources.String3, Source={StaticResource LocalizedStrings}}"/>
                    </TextBlock>
                    <Button Content="{Binding Path=LocalizedResources.String4, Source={StaticResource LocalizedStrings}}"
                Width="269"
                Height="73"
                FontSize="22"
                HorizontalAlignment="Left"
                Click="HTTPButton_Click"/>
                </StackPanel>

            </phone:PanoramaItem>
            <!--Panorama item one-->
            <phone:PanoramaItem Header="{Binding Path=LocalizedResources.String5, Source={StaticResource LocalizedStrings}}">
                <Grid>
                    <TextBlock HorizontalAlignment="Left" Margin="16,-22,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="487" Width="400" FontSize="24">
        	        <Run Text="{Binding Path=LocalizedResources.String6, Source={StaticResource LocalizedStrings}}"/>
                    </TextBlock>
                    <Button Content="{Binding Path=LocalizedResources.String7, Source={StaticResource LocalizedStrings}}" HorizontalAlignment="Left" Margin="10,100,0,0" VerticalAlignment="Top" Width="237" Click="EQ_Click" Height="74" RenderTransformOrigin="0.5,0.5"/>
                </Grid>
            </phone:PanoramaItem>
            <phone:PanoramaItem Header="{Binding Path=LocalizedResources.String8, Source={StaticResource LocalizedStrings}}">
                <StackPanel Margin="24">
                    <TextBlock Text="Choose your language:" FontSize="24" Margin="0,0,0,20"/>

                    <ListBox x:Name="LanguageListBox"
         SelectionChanged="LanguageListBox_SelectionChanged"
         Margin="0,12,52,12"
         Height="300"
         Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"
                       FontSize="24"
                       Foreground="White"
                       Padding="8"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                </StackPanel>
            </phone:PanoramaItem>
            <phone:PanoramaItem Header="{Binding Path=LocalizedResources.String9, Source={StaticResource LocalizedStrings}}">
                <Grid x:Name="LayoutRoot41" Margin="20">
                    <StackPanel>
                        <TextBlock x:Name="UpdateStatus" Text="Reserved" FontSize="22" Margin="0,0,0,10" Height="59"/>
                    </StackPanel>
                </Grid>
            </phone:PanoramaItem>
        </phone:Panorama>
        <MediaElement x:Name="AudioPlayer" AutoPlay="False" />

    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" Mode="Minimized">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="back to home" Click="Home_Click"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>