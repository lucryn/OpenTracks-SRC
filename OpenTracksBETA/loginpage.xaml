<phone:PhoneApplicationPage
    x:Class="OpenTracksBETA.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Orientation="Portrait">


    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot30" Background="Transparent">

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Panorama control-->
        <phone:Panorama Title="accounts">
            <phone:Panorama.Background>
                <ImageBrush ImageSource="/Assets/bg1.png"/>
            </phone:Panorama.Background>

            <!--welcome-->
            <phone:PanoramaItem Header="login" Width="447">
                <StackPanel Margin="10,-21,24,24">
                    <TextBlock Text="  Email:" FontSize="24" Foreground="White" />
                    <TextBox x:Name="EmailBox" />

                    <TextBlock Text="  Password:" FontSize="24" Foreground="White" Margin="0,12,0,0" />
                    <PasswordBox x:Name="PasswordBox" />

                    <Button Content="Login" Click="LoginButton_Click" Margin="0,24,0,0" />
                </StackPanel>
            </phone:PanoramaItem>
            <phone:PanoramaItem Header="register " Width="447">
                <StackPanel Margin="10,-20,20,20">

                    <TextBlock Text="Email:" FontSize="24" Margin="10,0,0,0"/>
                    <TextBox x:Name="RegisterEmailBox" />

                    <TextBlock Text="Password:" FontSize="24" Margin="10,0,0,0"/>
                    <PasswordBox x:Name="RegisterPasswordBox" />

                    <Button Content="Register"
                    Margin="10,20,0,0"
                    Click="RegisterButton_Click" />

                </StackPanel>

            </phone:PanoramaItem>
            <!--Panorama item one-->
            <phone:PanoramaItem Header="about">
                <TextBlock HorizontalAlignment="Left" Margin="16,-22,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="487" Width="400" FontSize="24">
        	    <Run Text="App Version: Stable 1.1"/>
                <LineBreak/>
                <Run Text="Update: AMOLED Update 1"/>
        	    <LineBreak/>
        	    <Run Text="*All Music Are Non-Copyrighted"/>
        	    <LineBreak/>
        	    <Run Text="OpenTracks for Windows Phone 8.1"/>
                <LineBreak/>
                <LineBreak/>
                <Run Text="Developed by:"/>
                <LineBreak/>
                <Run Text="ChockingNetDude"/>
                <LineBreak/>
                <LineBreak/>
                <Run Text="Please Sign in / Register a account to continue."/>
                </TextBlock>
            </phone:PanoramaItem>
        </phone:Panorama>
        <MediaElement x:Name="AudioPlayer" AutoPlay="False" />
    </Grid>
</phone:PhoneApplicationPage>